class Node {
    int data;     // Dado armazenado no nó
    Node next;    // Referência para o próximo nó

    // Construtor
    public Node(int data) {
        this.data = data;
        this.next = null;
    }
}